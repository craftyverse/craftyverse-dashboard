export class SignupPageUtils {
  /**
   * This utility function is to validate the user
   * @param userEmail
   * @returns
   */
  // static validateUserEmail(userEmail: string): string {
  //   return '';
  // }
}
